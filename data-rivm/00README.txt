RIVM data snapshots.

tests:

        GGD tests, daily snapshots. Most recent data will be
        auto-downloaded from RIVM website. Older missing data
        will be taken from ../../mzelst-covid19-nobackup git
        repository.

        Compressed csv files.

R_number:

        Snapshots of R number over time. Slightly preprocessed
        from RIVM version.

        
